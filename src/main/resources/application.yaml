server:
  port: 8080
  error:
    include-stacktrace: always

Spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/airport_management_db?createDatabaseIfNotExist=true
    username: root
    password:

